# Marketplace extension 0.6.0 — live test

**Marketplace:** `kotrolabs.kotro-proxy-engine@0.6.0` (updated 2026-07-19T03:50:30Z)  
**Installed into Cursor via:** local Marketplace VSIX package  

## Package checks
| Check | Result |
|-------|--------|
| Marketplace shortDescription mentions SHA-256 + Setup Wizard | PASS |
| VSIX has LICENSE.txt | PASS |
| No `auto-router.js` | PASS |
| Has `setup-wizard.js` + checksum `downloader.js` | PASS |
| Bundled binaries version `kotro-proxy 0.6.0` | PASS |
| Release `checksums.txt` matches darwin-arm64 tarball | PASS |

## Runtime
| Check | Result |
|-------|--------|
| Downloaded binary starts; `/healthz` OK | PASS |
| `/dashboard` 200 | PASS |
| Verify-Cache style: MISS then HIT | PASS (`x-kotro-cache: HIT` on 2nd) |
| Dashboard API: 1 hit / 1 miss, $ saved > 0 | PASS |
| Cursor CLI: extension installed `@0.6.0` | PASS |

## Gaps / notes
1. **51MB VSIX** still ships all platform binaries *and* downloader re-fetches to globalStorage — redundant but OK.
2. Cursor CLI `--install-extension kotrolabs.kotro-proxy-engine` failed (gallery ID resolve); **VSIX install worked**.
3. **First boot MiniLM load** — wait for `/healthz` (can be multi-second on cold cache).
4. **IDE Base URL** still manual — extension does not auto-route Cursor (by design).
5. Homebrew tap may still be 0.5.2 (separate from extension).
