# R0.1b topology result — 2026-08-07

**Status:** **PASS** with honesty note (`topology-20260807T062507Z.txt`)

| Check | Result |
|-------|--------|
| #16 agent → data-plane → upstream | PASS (`DATAPLANE_OK UPSTREAM_OK`) |
| Direct upstream from agent | PASS (blocked) |
| Provider token in agent env | PASS (absent) |
| #25 host canary / gateway | PASS (no CANARY_SECRET leak) |

### #25 (Sol rename)

**host canary content unreachable; gateway exposure recorded** — not “gateway reachability denied.”

- `host.docker.internal` did not resolve on `--internal` agent net.  
- Gateway `172.18.0.1:canary` returned **Connection refused** (not “network unreachable”).  
  That means the **gateway IP is L3-addressable** from the agent; nothing answered on that port.  
  **Do not** bind sensitive services on the bridge gateway. Claiming “sole window” still requires ensuring only the data-plane is reachable — not assuming `internal: true` hides the gateway.

### Option A staging

`stage-repo.sh` smoke on this repo: tracked archive @ pin, **no `.git`**, manifest written.

### Next

R0.2 mapping draft: `docs/roadmap/KOTRO-PERMIT-R0.2-MAPPING.md` (not frozen).  
Then R0.3/R0.4 or Gate A recruiting with R0.1a clip.
