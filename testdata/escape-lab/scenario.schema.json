{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://kotro.dev/schemas/escape-lab/scenario.v1.json",
  "title": "Kotro Escape Lab scenario",
  "description": "One reproducible agent-security scenario. A scenario declares an attack, the outcome the tool under test is expected to produce today, and the evidence that should exist afterward. Declared outcomes include honest no-coverage cases.",
  "type": "object",
  "required": ["id", "name", "category", "severity", "description", "attack", "expect"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^EL-[0-9]{2}$",
      "description": "Stable scenario identifier. Never reused or renumbered."
    },
    "name": { "type": "string", "minLength": 1 },
    "category": {
      "type": "string",
      "enum": [
        "injection",
        "secret-exfiltration",
        "resource-abuse",
        "tool-integrity",
        "operator-control",
        "egress",
        "monitoring-integrity",
        "persistence"
      ]
    },
    "severity": { "type": "string", "enum": ["low", "medium", "high", "critical"] },
    "description": { "type": "string", "minLength": 1 },
    "references": {
      "type": "array",
      "items": { "type": "string" },
      "description": "External mappings, e.g. OWASP Agentic Top 10 identifiers."
    },
    "env": {
      "type": "object",
      "additionalProperties": { "type": "string" },
      "description": "Proxy environment required for this scenario. The runner restarts the target when env differs from the previous scenario."
    },
    "attack": {
      "type": "object",
      "required": ["steps"],
      "additionalProperties": false,
      "properties": {
        "steps": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "required": ["method", "path"],
            "additionalProperties": false,
            "properties": {
              "method": { "type": "string", "enum": ["GET", "POST", "PUT", "DELETE"] },
              "path": { "type": "string" },
              "headers": {
                "type": "object",
                "additionalProperties": { "type": "string" }
              },
              "body": {
                "description": "JSON request body. Omitted for GET.",
                "type": ["object", "null"]
              },
              "repeat": {
                "type": "integer",
                "minimum": 1,
                "default": 1,
                "description": "Send this step N times. Used for loop and budget scenarios."
              }
            }
          }
        }
      }
    },
    "expect": {
      "type": "object",
      "required": ["outcome"],
      "additionalProperties": false,
      "properties": {
        "outcome": {
          "type": "string",
          "enum": ["prevent", "detect", "observe", "none"],
          "description": "prevent = attack blocked before effect. detect = allowed but flagged with evidence. observe = recorded without a security verdict. none = no coverage today (an honest, tracked gap)."
        },
        "http_status": {
          "type": "array",
          "items": { "type": "integer" },
          "description": "Acceptable final-step HTTP statuses."
        },
        "headers_present": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Response header names that must be present (case-insensitive)."
        },
        "headers_absent": {
          "type": "array",
          "items": { "type": "string" }
        },
        "body_contains": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Case-insensitive substrings that must appear in the final response body. Used where the coverage signal is a reporting endpoint rather than a flight event."
        },
        "flight_kind": {
          "type": "string",
          "description": "FlightEvent kind that must appear on the tape after the attack."
        },
        "flight_enforced": {
          "type": "boolean",
          "description": "Expected value of the enforced flag on the matching flight event."
        },
        "gap_reason": {
          "type": "string",
          "description": "Required when outcome is none. States why there is no coverage and which phase addresses it."
        }
      }
    }
  },
  "allOf": [
    {
      "if": {
        "properties": { "expect": { "properties": { "outcome": { "const": "none" } } } },
        "required": ["expect"]
      },
      "then": {
        "properties": {
          "expect": { "required": ["outcome", "gap_reason"] }
        }
      }
    }
  ]
}
